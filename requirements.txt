# --- Phase 1: core / twin ---
numpy>=1.26
scipy>=1.11
pandas>=2.1
matplotlib>=3.8
gymnasium>=0.29

# --- Phase 2: hybrid twin (residual LSTM) ---
torch>=2.2

# --- Phase 3: RL ---
stable-baselines3>=2.3

# --- Phase 4: uncertainty ---
scikit-learn>=1.4

# --- Phase 5: explainability ---
shap>=0.45
anthropic>=0.40   # optional: only needed for the LLM explanation layer;
                  # falls back to a template-based explainer if unset

# --- Phase 7: dashboard ---
streamlit>=1.33
plotly>=5.20

# --- Phase 2+ (optional, install later): high-fidelity simulation ---
# Sinergym (EnergyPlus-backed, optional) is intentionally NOT in this file --
# see requirements-sinergym.txt and docs/sinergym_setup.md. Installing it here
# would break `pip install -r requirements.txt` in CI: Sinergym's PyPI package
# depends on `opyplus` via a git URL, which most CI package resolvers reject.

# --- dev tooling ---
pytest>=8.0
pre-commit>=3.6
black>=24.0
ruff>=0.4
